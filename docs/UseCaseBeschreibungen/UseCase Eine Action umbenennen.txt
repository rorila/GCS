-- UseCase: Eine Action umbenennen
- Sichergehen, dass man sich in der Stage: Login befindet, wenn das nicht der Fall ist: über das Menü (Stages) die Stage: Login auswählen.
- Tab: Flow auswählen
- Sichergehen, dass man sich in dem Task: VerifyTask befindet, wenn das nicht der Fall ist: über das DropDown den Task: VerifyTask auswählen

-- UseCase: Action erzeugen
- von der Toolbox ein Action auf das Flowdiagramm ziehen
- Testen, ob die Action in der Action-Liste der Stage vorhanden ist. (Name: Action)
- Testen, ob die Action in der Manager-Liste (Actions) vorhanden ist.

-- UseCase: Action umbenennen
- Action muss markiert werden.  Properties im Inspector ausgewählt sein
- Action im Inspector umbenennen: von action nach VerifyAction

- Testen: Die globale Variable in der Blueprint-Stage 'isProjectChangeAvailable' muss nun 'true' sein.

- Testen ob der Actionname in den JSON-Daten geändert hat.(Name: VerifyAction). Die Action mit dem Namen: action darf nicht mehr vorhanden sein.
- Testen ob der Actionname in den Manager-Daten (Tasks) geändert hat.(Name: VerifyAction). Die Action mit dem Namen: action darf nicht mehr vorhanden sein.
