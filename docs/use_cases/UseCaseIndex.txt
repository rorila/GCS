UseCase Index
-------------
- [2026-03-02] ActionDeletionBugfix (v3.15.2): Vollständige Korrektur der Action-Löschung im Flow-Diagramm. Fix der Case-Sensitivity bei Node-Typen ('action' vs 'Action') in `FlowGraphManager.ts` und `ProjectRegistry.ts`. Korrektur der Referenzprüfung in `ProjectRegistry.ts`, um auch Kleinschreibung zu unterstützen. Konsolidierung der Lösch-Bestätigungsdialoge in `Editor.ts` zur Vermeidung von Doppel-Prompts. Verifiziert durch Deep-Trace-Browsertest.
- [2026-03-01] ActionDeletionFixV2 (v3.15.0): Korrektur des `deleteAction` Fehlers im `ActionRefactoringService.ts`, durch den `DataAction`-Knoten bei der Löschung übersprungen wurden. Vollständige Bereinigung der `project.json` von verwaisten `Action1/2` Definitionen in allen Stages (Login, Dashboard, Blueprint). Verifiziert durch erweiterten `action_crud.test.ts` und workspace-weiten Content-Scan.
- FinalDeletionSyncFix (v3.14.5): FlowGraphManager.ts, FlowTaskManager.ts (Sync-Muster)
DeletionSyncFix (v3.14.4): FlowGraphManager.ts
 ActionDropBugfix (v3.14.3): FlowSyncManager.ts, FlowDataAction.ts, FlowNodeFactory.ts
- [2026-03-01] ActionRegistrationFix (v3.14.2): Automatische Registrierung von Actions beim Drop auf den Flow-Editor via `FlowSyncManager.ts`. Einführung des neuen Logger-UseCase `Action_Lifecycle` für Erstellungs-, Lösch- und Umbenennungsvorgänge in `FlowSyncManager.ts` und `ActionRefactoringService.ts`. Verbesserung der UI-Synchronität im `Editor.ts` via Mediator-Refresh bei Projekt-Datenänderungen. Verifiziert durch Regressionstest `tests/action_registration.test.ts`.
- [2026-03-01] DeepRefactoringFix: Behebung von Namens-Reversionen im Flow-Editor durch Multi-Feld-Matching (hasOldNameMatch) in TaskRefactoringService.ts und ActionRefactoringService.ts. Verhindert Datenverlust durch verzögerte Namens-Zuweisung im FlowNodeHandler.ts (Aufschub bis zum globalen Refactoring). Korrektur der Zuweisungsreihenfolge im FlowSyncManager.ts (restoreNode) inklusive erzwungenem Visual-Refresh (setShowDetails) für konsistente Darstellung nach Neuladen.
- FlowConditionEditor: Flexibler Editor für Bedingungen im Flow-Editor. Unterstützt Operanden-Auswahl (Variable, Literal, Eigenschaft) via `inspector_condition.json` und spezialisierten `FlowConditionHandler.ts`. NEU: Support für Untereigenschaften von Objekt-Variablen (z.B. currentUser.name). Runtime-Evaluierung in `TaskExecutor.ts` via `PropertyHelper`. Fix: Flexibler `InspectorTemplateLoader` für verschiedene JSON-Strukturen.
- AIAgentController: Technische Schnittstelle (API) für AI-Agenten (`AgentController.ts`) zur sicheren Manipulation des Projekts. Erzwingt Validierungsregeln (keine Inline-Actions), korrekte Task-Registrierung (Global/Stage) und implementiert die "Scorched Earth" Strategie zur Flow-Regeneration (`invalidateTaskFlow`).
- ActionParameterStandardisierung: Umstellung auf OOP-Struktur und 1:1 Mapping von UI zu Modell.
- SaveAction: (Teil von ActionParameterStandardisierung) Der Prozess der Werterfassung und Bereinigung vor dem Speichern.
- FlowEditorDetailAnzeige: Wie Aktionen im Flow-Editor visualisiert werden und woher die Detail-Strings kommen.
- ActionVisualisierungsSynchronisation: Konsistente Anzeige von Action-Details durch Nutzung des JSON-Feldes 'details' als Single Source of Truth.
- SynchronisationLoeschenFlowEditor: Sicherstellung, dass beim Löschen einer Action im Editor alle Referenzen im Projekt (Tasks, Stages) entfernt werden.
- SofortigeJSONSynchronisation: Mechanismus für unmittelbare Sichtbarkeit von Änderungen (Trinity-Sync). Dokumentiert zudem strukturelle Unterschiede (Branching vs. Linear) und das Smart-Delete-Verfahren für verwaiste Aktions-Definitionen.
- LoginFlow: Detaillierter technischer Ablauf des Logins (Client -> API-Sim -> Server -> DB -> Client) mit Referenzen zu allen beteiligten Methoden und Stack-Trace.
- QA_Report: Automatisierter Test-Bericht (Dashboard) mit Mermaid-Visualisierung der aktuellen Regression-Suite (Gut- und Schlecht-Tests).
- DataModels: Definition der JSON-Strukturen (User, Rollen) für konsistente Verarbeitung und Typ-Klarheit.
- SmartMapping: Entpacken verschachtelter API-Antworten (Result Selector) und dynamische Entdeckung von Datenbank-Modellen im Editor.
- VariablentypPersistenz: Lösung des Persistenz-Problems durch Unifizierung auf 'type' und Korrektur der Serialisierung (reservedKeys).
- VariableMorphing: Detaillierte technische Dokumentation des Morphing-Prozesses (Integer -> Object), inklusive ID-Persistenz und Inspector-Fix.
- executeCalculateVariable: Detaillierter Ablauf der Variablen-Berechnung, Proxy-Robuste Evaluierung und Vermeidung von 'undefined' in Strings.
- EmojiPinFlow: Detaillierte technische Dokumentation des `createAnEmojiPin` Tasks (Stage -> Runtime -> Action/Events), Payload-Format (String vs Object) und wichtige Dateien.
- DataActionInspector: Dokumentation der UI-Konstruktion für `DataAction`. Erklärt die Diskrepanz zwischen `StandardActions.ts` (Logic Metadata) und `inspector_data_action.json` (Visual Layout) sowie die dynamische Befüllung von Dropdowns (`availableDataStores`) via `InspectorHost.ts`. Beinhaltet zudem die Lösung für die dauerhafte Verfügbarkeit von Helper-Funktionen (`getRouteSuggestions`) durch Re-Registrierung in der `update()`-Schleife.
- DataStorePersistence: Technische Dokumentation der TDataStore-Anbindung. Beinhaltet die OOP-Getter/Setter in FlowDataAction.ts, die Robuste Smart-Sync-Logik in InspectorHost.ts (handleAutoSave,...) und die Erweiterung des ApiSimulator in Editor.ts (request).
- GenericVariableTypes: Einführung der Variablentypen `any` und `json` in `types.ts` zur Unterstützung modell-freier Datenstrukturen. Behebung der Typ-Persistenz in `Editor.ts` durch explizite Typ-Zuweisung während des Morphings.
- SektorSelektion: Implementierung des rekursiven JSON-Scans im Backend (server.ts) und Integration der Sektorauswahl (Dropdown + Gesamte Daten) im InspectorRenderer.ts. Behebung von Persistenz-Problemen durch Synchronisation von FlowDataAction-Definitionen.
- VariableValuePersistence: Behebung des Fehlers beim Zuweisen von Werten in String-Variablen durch Korrektur der Auflösungs-Logik in PropertyHelper.ts. Verhindert premature Variable-Resolution beim Zugriff auf.value/.name.
- SmartAccess: Automatisches Unwrapping von Ein-Element-Arrays in `PropertyHelper.ts` für bequemen Datenzugriff (z.B. `${currentUser.name}`) sowie präzise Query-Filterung im `ApiSimulator` (Editor.ts).
- [2026-02-18] JWTAuthenticationFlow: Implementierung der JWT-Unterstützung in der `http` Action (`StandardActions.ts`) inklusive automatischer Token-Injektion und expliziter Fehler-Verzweigung (Rückgabe von `false` bei 401/Sim-Error). Dokumentation des visuellen Unterschieds zwischen `Action` (orange) und `DataAction` (blau) Nodes in den `DEVELOPER_GUIDELINES.md` und Erstellung eines Umsetzungsplans zur Fehlerbehandlung im Flow-Editor.
- [2026-02-18] DataActionInspectorFix: Behebung der fehlerhaften Inspector-Anzeige für `DataAction` Nodes. Durch Hinzufügen von `isFlowNode` in `FlowElement.ts` und Anpassung des `FlowNodeHandler.ts` wird nun das korrekte spezialisierte Template `inspector_data_action.json` geladen, welches Geometrie/Style ausblendet und fachliche Felder (URL, Method, resultVariable) anzeigt.
- UniversalSmartUnwrap: Umstellung auf Unwrapping an der Datenquelle (`StandardActions.ts`) für konsistente Speicherung als Objekt. Vereinheitlichung der Pfad-Auflösung via `PropertyHelper` in `ExpressionParser.ts`.
- [2026-02-27] ModularisierungUndHygiene: In Phase 2 wurden die monolithischen Klassen `RefactoringManager.ts` und `TaskExecutor.ts` modularisiert. Refactoring-Logik wurde in spezialisierte Services (`VariableRefactoringService`, `TaskRefactoringService`, etc.) in `src/editor/refactoring/` extrahiert. Runtime-Logik (Conditions/Loops) wurde in `TaskConditionEvaluator.ts` und `TaskLoopHandler.ts` in `src/runtime/executor/` ausgelagert. Ziel: Einhaltung des 1000-Zeilen-Limits und Verbesserung der Wartbarkeit.
- [2026-02-24] TaskVisibilityConsolidation: Lösung des Problems verschwindender Tasks. `FlowEditor.ts` (`updateFlowSelector`) zeigt nun permanent die Gruppe "Global / Blueprint" an. `RefactoringManager.ts` (`sanitizeProject`) bereinigt automatisch Task-Duplikate über Stage-Grenzen hinweg. Deletion-Logik in `deleteTask` wurde auf Case-Insensitivity umgestellt, um Fehler bei unterschiedlicher Schreibweise (AttemptLogin vs attemptLogin) zu vermeiden. AttemptLogin wurde in stage_login konsolidiert.
- UC-001: DeletionLogicFixV2 - Umfassende Wiederherstellung der Löschfunktion. Behebt das Problem blockierter Flow-Knoten durch Entschärfung des `isLinked` Checks in `FlowGraphManager.ts`. Implementierung von `removeMultipleObjectsWithConfirm` in `Editor.ts` für konsistente Referenzprüfung bei Mehrfachselektion. Rekursive Löschung verschachtelter Objekte in `EditorCommandManager.ts` (`removeObjectSilent`) via `findParentContainer`. (FlowGraphManager.ts, Editor.ts, EditorCommandManager.ts, EditorInteractionManager.ts)
- UC-002: DraggingPrecisionFix - Behebung unpräziser Komponenten-Bewegungen auf der Stage. Implementierung einer skalisierungsunabhängigen Koordinatenberechnung (`getRelativeCoordinates`) und eines DOM-Element Caching-Mechanismus (`dragElements`) zur Performancesteigerung während `mousemove` Events. Korrektur der Skalierung für Resizing, Rectangle Selection und Komponenten-Drop aus der Palette. (StageInteractionManager.ts)



- DataServiceSmartMatch: Erweiterung der Filter-Logik im `DataService.ts` zum Vergleich von String-Queries gegen Array-Daten (Emoji-PINs).
- MagicVariablePicker: Automatisches Dropdown (📦) für `TEdit` Komponenten (`InspectorRenderer.ts`), befüllt aus `availableVariablesAsTokens`. Kapselung in `renderEditWithVariablePicker`.
- BlueprintPersistenceSafety: Implementierung eines Safety-Checks in `Editor.ts` (syncStageObjectsToProject), der das Überschreiben der Blueprint-Stage mit leeren Objekt-Listen durch "Race Conditions" beim Speichern verhindert.
- BlueprintVariableResolution: Aktualisierung des `RuntimeVariableManager` (initializeVariables), um Variablen aus `stage_blueprint` als primäre Quelle für globale Variablen zu laden. `project.variables` wurde als Legacy-Fallback eingestuft.
- ActionConfigDynamicUI: Ermöglicht die Konfiguration spezifischer Action-Typen (wie 'http', 'store_token') im Inspector, indem `FlowEditor.ts` (createNode) den Subtype aus dem Toolbox-Item extrahiert und in `node.data.type` speichert. `InspectorHost.ts` (update) nutzt dies, um für diese Typen dynamisch die `getInspectorProperties`-Methode der `FlowAction`-Klasse aufzurufen, anstatt das statische `inspector_action.json` Template zu verwenden.
- Phase 23: Synchronisation Task-Löschung
Dateien: TaskRefactoringService.ts, EditorCommandManager.ts, refactoring_manager.test.ts
Methoden: deleteTask, cleanupEvents, removeObject
Status: Abgeschlossen (Bereinigung von Objekt-Events und Case-Sensitivity Fix)
- [2026-02-17] OOInspectorArchitektur: Refactoring zu modularer Architektur (InspectorHost, Renderer, Registry). Fix: Initialisierung via DesignRuntime, Fix: Flickering im FlowEditor durch Selection-Sync. Neu: Spezialisierte Minimal-Templates für Flow & Variablen zur Reduktion von UI-Clutter. Refinement: Task-Inspector bereinigt (Name-Binding Fix, Geometrie/Parameter entfernt, Button-Renderer Fix). Fix: Crash bei Task-Umbenennung in `RefactoringManager.ts` behoben (Array-Check). (Editor.ts, InspectorHost.ts, FlowNodeHandler.ts, VariableHandler.ts, FlowTask.ts, FlowElement.ts, InspectorRenderer.ts, RefactoringManager.ts)
- DynamicDropdowns: Dokumentation der Implementierung dynamischer Dropdowns in Inspector und Dialogen via `source`-Property und `ProjectRegistry` zur Vermeidung von Stale-Data. (InspectorRenderer.ts, JSONDialogRenderer.ts)
- FlowEditorRenameSync: Verhinderung von Reload-Loops/View-Resets beim Umbenennen von Tasks durch Event-Filterung in `Editor.ts` und robustes Context-Update in `FlowEditor.ts` mit explizitem Reload (`loadFromProject(newName)`). Korrektur der Event-Propagation in `InspectorHost.ts` und explizites lokales Update in `FlowNodeHandler.ts` (Nutzung des `Name`-Setters statt read-only `name`-Getter) gegen UI-Reverts und TypeErrors. (Editor.ts, FlowEditor.ts, InspectorHost.ts, FlowNodeHandler.ts)
- [2026-02-18] InspectorDataActionDropdowns: Implementierung des `InspectorContextBuilder.ts` zur dynamischen Befüllung von Dropdown-Listen in spezialisierten Inspector-Templates. Löst das Problem leerer Datenquellen (DataStores, Variablen-Tokens) beim Bearbeiten von DataActions (blauen Nodes) durch Injektion eines angereicherten Datenkontexts in `InspectorHost.ts` (resolveValue).
- [2026-02-18] SmartVariableInspector: Implementierung kontextsensitiver Felder (visible-Property) und specialized Event-Tabs in InspectorHost.ts, inspector_variable.json, inspector_variable_events.json. Fix für Tab-Switch Persistenz. Neu: Support für Schwellwert-Events (onThresholdReached/Left/Exceeded).
- [2026-02-18] JWTLoginFlowFix: Behebung des Fehlers, bei dem JWT-Anfragen als GET / ausgeführt wurden. Ursache war eine unvollständige Persistenz bei verlinkten Aktionen. Lösung: Runtime-Safety in StandardActions.ts erzeugt Defaults für URL/Methode bei aktivem requestJWT. FlowDataAction.ts synchronisiert das Property nun aggressiv in das lokale Datenmodell, um Konsistenz im actionSequence-Taskflow zu garantieren.
- [2026-02-18] DataActionPersistence: Robuste Eigenschafts-Synchronisation für DataActions. Einführung von "Local Mirroring" in FlowDataAction.ts, sodass alle Setter sowohl globale als auch lokale Daten aktualisieren. Implementierung von Legacy-Fallbacks im Editor (resource -> dataStore) und in der Runtime (property, variable, value) in StandardActions.ts, um Kompatibilität mit älteren Projekt-Dateien sicherzustellen.
- [2026-02-19] LoginFixArchitectureCleanup: Vollständige Bereinigung der Login-Logik und Behebung des authCode-Fehlers. Verschiebung von `AttemptLogin` (Task) und `doTheAuthenfification` (Action) in die globale `stage_blueprint`. Korrektur der `currentPIN`-Auflösung durch Entfernung des fehlerhaften `global.` Präfixes. Einführung der Force-Reload-Logik in `Editor.ts` und `ProjectPersistenceService.ts` zur Umgehung von LocalStorage-Altlasten. (Editor.ts, ProjectPersistenceService.ts, project.json, menu_bar.json)
- [2026-02-19] GlobalVariableLookup: Refactoring des `RuntimeVariableManager` (initializeVariables), um beim Start einen Index aller globalen Variablen aus *allen* Stages (inkl. Blueprint) aufzubauen. Behebt Cross-Stage-Lookup Probleme (z.B. Blueprint-Variable in Login-Stage).
- [2026-02-19] ReactiveBindingPriority: Korrektur der Priorisierung von Variablen-Werten gegenüber Komponenten-Objekten in `ReactiveRuntime` (getContext). Sicherstellung, dass Updates via `RuntimeVariableManager` immer unter dem Variablennamen (`actualProp`) erfolgen, damit Bindings wie `${currentUser.name}` den echten Wert auflösen.
- [2026-02-19] RobusteStageNavigation: `navigate_stage` nutzt jetzt primär `TStageController.goToStage()` (StandardActions.ts) statt nur `onNavigate`. Fallback im `UniversalPlayer.handleNavigation()` (player-standalone.ts) um `stage:`-Prefix erweitert. Behebt Bug: Standalone-Player konnte Stage-Wechsel nicht durchführen.
- [2026-02-19] BlueprintFlowVisualisierung: Integration der automatischen Mermaid-Diagramm-Anzeige für Blueprint-Stages im Editor. Dedizierter Overlay-Container `#blueprint-viewer` in `Editor.ts` und spezifisches Styling in `style.css`. Eliminierung globaler Action-Duplikate in `project.json` zur Datenkonsistenz. (Editor.ts, FlowDiagramGenerator.ts, project.json)
- [2026-02-19] Architecture-Blueprint-as-SSoT: Umstellung der Engine auf die Blueprint-Stage als alleinige Quelle für globale Tasks, Aktionen, Variablen und Services. Ermöglicht den Verzicht auf Wurzel-Arrays in `project.json`. Anpassung der Auflösungs-Logik in `ProjectRegistry.ts`, der Sichtbarkeit in `Editor.ts` und der Automatisierung im `AgentController.ts`. (ProjectRegistry.ts, Editor.ts, AgentController.ts, project.json)
- [2026-02-19] F_BlueprintVisibilityFix: Korrektur der Sichtbarkeits-Regression in Editor.ts durch strikte Trennung von Blueprint- und Normal-Stages. Einführung eines dedizierten Flow-Views für Mermaid-Diagramme (Editor.ts), sodass die Blueprint-Stage grafisch bearbeitbar bleibt. Behebung von Zombie-Objekten beim Stage-Wechsel durch Anpassung der switchStage-Sequenz (Runtime Stop vor Render). (Editor.ts, FlowDiagramGenerator.ts)
- [2026-02-19] StageFlowTabTrennung: Behebung des Fehlers, bei dem Stage-Elemente (z.B. Login-UI) auch im Flow-, JSON-, Pascal- und Manager-Tab sichtbar waren. Ursache: `stage-wrapper` wurde in `Editor.ts` (`render()`, L465-470) im `else`-Zweig bedingungslos auf `display: flex` gesetzt, unabhängig vom aktiven Tab. Fix: `isStageOrRunView`-Check sichert, dass `stage-wrapper` nur bei `currentView === 'stage'` oder `'run'` eingeblendet wird. (Editor.ts, render(), L465-470)
- [2026-02-19] BlueprintFlowTabFix: Entfernung des statischen Mermaid-Diagramm-Viewers (`#blueprint-viewer`, `renderFlowDiagram`) für Blueprint-Stage. Blueprint-Stage nutzt nun den normalen interaktiven FlowEditor (`#flow-viewer`) im Flow-Tab, identisch zu allen anderen Stages. (render() L458-471, Initialisierung L1786-1796 entfernt)
- [2026-02-19] BlueprintFlowDropdownFix: In `FlowEditor.ts` `updateFlowSelector()`: Im Blueprint-Zweig werden nun zuerst `activeStage.flowCharts` und `activeStage.tasks` (stage_blueprint) aufgelistet. Damit erscheinen Blueprint-eigene Tasks (z.B. AttemptLogin) im Dropdown. Legacy `project.tasks`/`project.flowCharts` folgen danach als Fallback. (FlowEditor.ts, updateFlowSelector(), L535-605)
- [2026-02-19] DataActionKnotenTypFix: In `FlowSyncManager.ts` 3 Bugs gefixt, die dazu führten, dass DataActions (type:'data_action') nicht als DataAction-Knoten (blau, mit Success/Error) im Flowdiagramm dargestellt wurden. Bug1: restoreNode() upgradet 'Action'-Knoten zu FlowDataAction wenn globale Def type:'data_action' hat. Bug2: generateFlowFromActionSequence() erzeugt 'DataAction'-Typ für data_action-items. Bug3: syncTaskFromFlow() schlägt echten Aktionstyp der verlinkten globalen Aktion nach statt pauschal 'action' zu schreiben. Zusätzlich: platform/project.json FlowChart-Element und actionSequence von AttemptLogin direkt gepatcht.
- [2026-02-20] UniversalActionConfig: Einführung eines generischen Systems zur Aktionskonfiguration. Nutzt die TActionParams-Komponente in InspectorRenderer.ts und JSONDialogRenderer.ts. Ermöglicht dynamische Methodenparameter via MethodRegistry.ts und Typwechsel im Inspector (ActionTypeSelect Mapping in InspectorEventHandler.ts). Bereinigung der dialog_action_editor.json durch Umstellung auf TActionParams.
- [2026-02-20] UniversalActionBindingFix: Korrektur des Daten-Bindings für Action-Parameter im Inspector. Durch Erweiterung von `PropertyHelper.ts` erfolgt nun ein automatischer Fallback auf das `.data`-Objekt von FlowNodes. Zudem stellt ein neuer `params`-Getter in `FlowAction.ts` sicher, dass auch Parameter verlinkter Aktionen (Linked Mode) korrekt über den Inspector editiert werden können.
- [2026-02-20] VariablePickerRecovery: Wiederherstellung des V-Buttons im Inspector für String-Props und Action-Params. Unterstützung für Untereigenschaften (Dot-Notation) und Modell-Hinweise hinzugefügt. Variablen-Bindung Fix (Auflösung im Inspector/Editor) durch Kontext-Erweiterung (`InspectorContextBuilder`) und Scope-Präfix Support abgeschlossen.
Participating Files: InspectorRenderer.ts, InspectorActionHandler.ts, InspectorContextBuilder.ts, Editor.ts
- GlobalVariablePersistence: Lösung des Variablen-"Gedächtnisverlusts" beim Stage-Wechsel. Durch die optionale Aufspaltung in `clear(clearVariables: boolean)` behält die `ReactiveRuntime.ts` ihre Variablen bei. Ergänzt durch die `syncAllToReactive` Methode im `RuntimeVariableManager.ts`, um nach Initialisierung das globale Neuregistrieren der Variablen im Proxy zu gewährleisten.
- [2026-02-20] LiveModeStageNavigation: Behebung des Komplett-Neustarts der Engine beim Stage-Wechsel (`onNavigate`) im Live-Modus. Anpassung in `Editor.ts` (`switchStage`) mit neuem Flag `keepView`, um nicht implizit in die Visuelle Ansicht zu wechseln. Ergänzung von `switchToStage()` in `GameRuntime.ts` sowie korrekte Delegierung im `EditorRunManager.ts`, wodurch die Runtime bewahrt wird und Variablenkonsistenz gewahrt bleibt.
- [2026-02-21] StageEvents support: Exponierung von Stage-Events (`onEnter`, `onLeave`, `onRuntimeStart`) im Inspector via `inspector_stage_events.json` and `StageHandler.ts`. Erweiterung des `EditorCommandManager.ts` zur automatischen Stage-Selektion bei Deselektion von Objekten. Ermöglicht JWT-basierten Datenabruf beim Stage-Wechsel via `onEnter`-Trigger.
- [2026-02-21] ToolboxExpansion (Data & Auth): Hinzufügen der Kategorie "Daten & Auth" zu `toolbox.json` und `toolbox_horizontal.json`. Enthält `TDataStore` (Database), `TAuthService` (Auth Service) und `TUserManager` (User Manager), um die Konfiguration von Datenabrufen (z.B. Räume pro Admin) direkt aus der Toolbox zu ermöglichen.
- [2026-02-21] SubPropertyDropdownSupport: Erweiterung des `InspectorContextBuilder.ts`, um Untereigenschaften komplexer Variablen (z.B. `currentUser.id`) automatisch in Dropdown-Listen anzuzeigen. Tokens werden mit Einrückung und vollem Pfad (`  variable.prop`) generiert, für maximale Klarheit im selektierten Zustand.
- [2026-02-21] ScopedVariableResolutionFix: Ergänzung von `global` und `stage` Roots im `RuntimeVariableManager.ts`, um Kompatibilität mit den vom Inspector generierten Tokens sicherzustellen und eine korrekte Auflösung zur Laufzeit zu gewährleisten.
- [2026-02-21] StageEventAssignmentFix: Behebung des Fehlers, bei dem globale Tasks nicht in Stage-Events zugewiesen werden konnten. Korrektur der `source`-Auflösung im `InspectorHost.ts` und Ergänzung von `TSelect` Unterstützung.
- [2026-02-21] Room-Data-Fetch-Fix: db.json, project.json - data schema flattened, query properties and URLs corrected.
- [2026-02-21] Inspector-Regression-Fix: Behebung leerer Dropdowns durch Auflösung von Ausdruck-Optionen (`${...}`) in `InspectorHost.ts`. Verbessertes Property-Mapping für `FlowNodes` in `PropertyHelper.ts`.
- [2026-02-21] Room-Data-Sync-Fix: RuntimeVariableManager.ts um `.data`-Sync für TObjectList erweitert. StandardActions.ts Auto-Unwrapping auf JWT-Login beschränkt. project.json Spalten von `currentRooms` auf Room-Felder korrigiert.
- [2026-02-21] DebugLogViewer-Improvements: `GameRuntime.ts` filtert ungenutzte onStart-Events raus und loggt onEnter. `PropertyWatcher.ts` unterdrückt Noise wie `eventCallback`. `StandardActions.ts` loggt HTTP-zu-Variable Zuweisungen. `RuntimeVariableManager.ts` nutzt Variablennamen anstatt IDs in der Log-Ausgabe.
- [2026-02-21] DebugLogViewer-StageNavigateFix: `EditorViewManager.ts` blockiert nun nicht mehr fälschlicherweise das Debug-Logging (`h.debugLog.setRecordingActive(false)`), wenn die Stage über `navigate_stage` gewechselt wird (`view === 'run'`).
- [2026-02-21] GlobalElementPersistenceFix: `RuntimeStageManager.ts` wurde modifiziert, um Blueprint- und Main-Stages zu cachen (via `cachedGlobalObjects`). Dadurch wird `hydrateObjects()` beim Stage-Wechsel nur noch für lokale Stage-Objekte aufgerufen. Dies verhindert den Zustandverlust von globalen Variablen (z.B. Session-Daten), da diese nicht bei jedem Wechsel neu instanziiert werden.
- [2026-02-21] Dynamic Table Component: `TTable.ts` als neue UI-Komponente angelegt. Fix: Modulares Template-Loading via `getInspectorFile()` (TTable.ts). Fix: `TDropdown` mit Variablen-Binding `${availableVariablesAsTokens}` in `inspector_table.json`. Fix: Intelligente Typ-Konvertierung (JSON/Number/Boolean) in `PropertyHelper.ts` und Anwendung im `InspectorEventHandler.ts`, um komplexe UI-Inputs (z.B. Spalten) korrekt als Objekte zu speichern. Toolbox-Kategorie auf "Erweitert" korrigiert.
- [2026-02-22] TDataAction SQL-transformation: Umstellung der DataAction auf SQL-Query-Logik mit Feld-Projektion. Erweiterung von `types.ts` (selectFields), Umbau von `inspector_data_action.json` und Implementierung der Projektions-Logik in `StandardActions.ts`.
- [2026-02-22] DynamicCardGallery: Implementierte Gallerie-Ansicht (displayMode: 'cards') in TTable.ts/Stage.ts, Migration stage_room_management in project.json.
- VariableInspectorFix: Behebung des Property-Auflösungs-Bugs in PropertyHelper.ts. Ermöglicht Metadaten-Zugriff (type, defaultValue) bei gesetzten Variablen-Werten.
- DataActionNaming: Erweiterung des DataAction-Inspectors (`inspector_data_action.json`) um Name und Beschreibung.
UseCase: TaskExecutionHierarchy (File: TaskExecutor.ts, Method: resolveTask)
UseUC-3.3.23-01: TaskExecutionHierarchy - Priorisierung der Blueprint-Stage in FlowEditor.ts und FlowSyncManager.ts
UC-3.3.23-02: ProjectConsolidation - Umzug globaler Tasks/Actions in stage_blueprint und SSoT-Cleanup
UC-3.3.23-03: Rule3Cleanup - Entfernung von Inline-Actions und redundanter Daten in project.json
UseCase: FlowSynchronizationFix (Files: FlowEditor.ts, FlowSyncManager.ts, Lines: Hierarchical Task Resolution & Cleanup)
UseCase: ActionInliningCleanup (File: project.json, Tasks: AttemptLogin, GetRoomAdminData)
- [2026-02-19] ActionSyncFix: SSoT-Konsolidierung in `project.json` (Verschiebung globaler Tasks in Blueprint). Implementierung von `POST /api/dev/save-project` in `server.ts` zur Datei-Persistenz. Integration in `Editor.ts` via `fetch`.
- [2026-02-23] VariableDeletionSync: Implementierung der intelligenten Lösch-Synchronisation für Variablen im Flow-Editor. Beinhaltet die projektweite Nutzungsprüfung (`RefactoringManager.getVariableUsageCount`) und die globale Löschlogik (`RefactoringManager.deleteVariable`). Integration in `FlowEditor.ts` via `deleteNode` (Smart Delete Dialog). Stellt die Konsistenz zwischen visuellen Diagramm-Knoten und dem Projektmodell (Blueprint-Stage) sicher. (RefactoringManager.ts, FlowEditor.ts)

- [2026-02-23] ProjectValidator: Implementierung eines automatisierten Validierungs-Scripts (`validate_project.cjs`) zur Sicherstellung der Datenkonsistenz in `project.json`. Prüft Events gegen Task-Definitionen, Actions gegen globale/lokale Listen sowie Ressourcen-Referenzen (Variablen & Komponenten) in Pascal-Ausdrücken. Beinhaltet Synchronitätsprüfungen zwischen Flow-Diagrammen und Logik-Sequenzen. (scripts/validate_project.cjs, project.json, package.json)
- [2026-02-23] LoginValidatorFix: Reorganisierung der Login-Tasks (`AttemptLogin`, `createAnEmojiPin`) und zugehöriger Aktionen von `stage_blueprint` in die `stage_login`. Behebung von Validierungsfehlern durch Definition globaler Aktionen (`GotoDashboard`, `SendApiResponse`) in der `stage_blueprint` und Eliminierung redundanter Flow-Diagramme via `fix_project_ssot.cjs`. (project.json, fix_project_ssot.cjs)
- [2026-02-23] ActionInspectorRefactoring: Ablösung des modalen Action-Editors durch den modularen `InspectorHost`. Implementierung von `getInspectorProperties` in `FlowAction.ts` and `FlowDataAction.ts` für dynamische Datenfelder. Integration der projektweiten Umbenennung (`RefactoringManager.renameAction`) beim Editieren des Namens im `InspectorHost.ts`. (FlowEditor.ts, InspectorHost.ts, FlowAction.ts, FlowDataAction.ts)
- [2026-02-23] FlowEditorModularisierung (Phase 2 & 3): Umfassende Refaktorisierung von `FlowEditor.ts` zur Verbesserung der Wartbarkeit und Reduzierung der Code-Komplexität (Ultra-Lean Editor). Kernfunktionalitäten wurden in spezialisierte Manager-Klassen extrahiert: `FlowGraphManager` (Graph-Operationen), `FlowInteractionManager` (Canvas-Events), `FlowSyncManager` (Model-Synchronisation), `FlowSelectionManager` (Fokus-Logik), `FlowMapManager` (Übersichten), `FlowContextMenuProvider` (Menüs), `FlowNavigationManager` (Navigation & History), `FlowGraphHydrator` (Laden & Importieren), `FlowUIController` (Grid & UI-Status), `FlowTaskManager` (Task-Flow-Management) und `FlowNodeFactory` (Knoten-Erstellung). (FlowEditor.ts, editor/services/*)
- [2026-02-23] StageRefactoring (Phase 1 & 2): Grundlegende Modularisierung der Stage.ts zur Reduktion der Komplexität. Auslagerung der Rendering-Logik in StageRenderer.ts und der Interaktions-Logik (Drag/Resize, ContextMenu, Selection) in StageInteractionManager.ts. Stage.ts fungiert nun als schlanker Orchestrator, der die Interfaces StageHost und StageInteractionHost implementiert und die Aufgaben delegiert. (Stage.ts, editor/services/StageRenderer.ts, editor/services/StageInteractionManager.ts)
- [2026-02-23] EditorModularisierung (Phasen 1-6): Umfassende Refaktorisierung von Editor.ts zur Reduktion der Komplexität (Ultra-Lean Editor). Die Datei wurde von ~2860 Zeilen auf ca. 280 Zeilen reduziert. Kernfunktionen wurden in 8 spezialisierte Manager-Klassen ausgelagert: EditorDataManager, EditorSimulatorManager, EditorRenderManager, EditorMenuManager, EditorKeyboardManager, EditorUndoManager, EditorInteractionManager und EditorStageManager. Editor.ts fungiert nun als reiner Orchestrator.
- [2026-02-23] EditorTypeScriptFixes: Vollständige Bereinigung der TypeScript-Fehler (ca. 40+) nach der Modularisierung von Editor.ts. Beinhaltet die Implementierung von Delegate-Methoden (switchStage), Fixes für implizites 'any' in EditorRenderManager.ts (o: any, obj: any), Korrektur der Event-Löschung (restoreStageEvents -> this.editor.stage.onEvent = null) im EditorRunManager.ts und die Entfernung zahlreicher verwaister Importe in AgentController.ts und StageRenderer.ts. Erweitertes Interfaces-Mapping für EditorHost.
- [2026-02-24] [v3.5.5] [DataAction Inspector SSoT] Lösung des Synchronisationsproblems bei DataActions. Implementierung einer robusten Namens-basierten Auflösung in FlowAction.ts und FlowDataAction.ts, die Aktionsdefinitionen aus dem Projekt auch ohne isLinked-Flag automatisch verknüpft. Ermöglicht sofortiges Laden vorhandener Daten beim Umbenennen von Knoten und sichert die Datenkonsistenz (Single Source of Truth) durch automatisches Setzen des isLinked-Status auf true.
- [2026-02-24] AttemptLoginRelocation: Vollständiger Umzug des `AttemptLogin` Tasks und der zugehörigen Aktionen (`httpLogin`, `doTheAuthenfification`, `GotoDashboard`, `ShowLoginError`, `ClearPIN`) sowie des Flow-Diagramms von der `stage_blueprint` in die `stage_login` in `project.json`. Korrektur der Persistenz-Probleme durch atomare Bereinigung der Blueprint-Stage. Validierung via `npm run test` (QA Report grün).
## [3.5.8] - 2026-02-25
- **Fix (Build)**: TypeScript-Fehler in `RefactoringManager.ts` behoben (ungenutzte Variable `originalCount` entfernt).
- **Validierung**: Vollständiger Projekt-Build und Regression-Tests erfolgreich durchgeführt. QA-Report ist grün.

## [3.5.7] - 2026-02-24
- **Fix (Persistenz)**: Lösung des "Geister-Tasks" Problems. `AttemptLogin` wurde endgültig aus der `stage_blueprint` entfernt und konsistent in der `stage_login` implementiert.
- **Integrität**: Bereinigung verwaister Aktionen und FlowCharts in der Blueprint-Stage zur Vermeidung von Layout-Konflikten.

- [2026-02-24] BackendProjectSync: Automatische Synchronisation der `project.json` vom Server beim Start des Editors. Überschreibt den lokalen `localStorage` (gcs_last_project), um Konsistenz zu garantieren. Implementiert in `Editor.ts` (`tryRestoreLastSession`) via `projectPersistenceService.fetchProjectFromServer`.
- [2026-02-24] ApiSimulationProxy: Umstellung des `ApiSimulator` im `EditorSimulatorManager.ts` auf Proxy-Betrieb. Anfragen an `/api/platform/login` und `/api/data/*` werden per `fetch` direkt an den Server delegiert, wodurch die `db.json` auf der Server-Platte die Source-of-Truth bleibt. Behebt Login-Fehler im Run-Mode.
- [2026-02-24] AutoViewSwitchOnStageSelection: Implementierung eines automatischen Wechsels zur visuellen Stage-Ansicht bei Selektion oder Neuanlage einer Stage. Zentralisiert in `Editor.ts` (`switchStage`). Zusätzlich Fix der Blueprint-Flow-Anzeige im Flow-Editor (Dropdown & Hydration).
- [2026-02-25] ProjectBuildValidation: Durchführung eines vollständigen Projekt-Builds (`npm run build`) und Validierung via `npm run test`. Behebung eines TypeScript-Linting-Fehlers in `RefactoringManager.ts` durch Entfernung einer ungenutzten Variable. Sicherstellung der Fehlerfreiheit für die aktuelle Session. (RefactoringManager.ts, QA_Report.md)
- [2026-02-25] DynamicModelDiscovery: Implementierung der dynamischen Modell- und Feld-Erkennung im `InspectorContextBuilder.ts`. Das System bezieht verfügbare Sektionen nun direkt aus dem `LocalStore` (via `DataService.getModelsSync`) und unterscheidet kontextsensitiv zwischen Singular (`object`) und Plural (`object_list`). Dies ermöglicht automatisches IntelliSense für alle Datenbank-Attribute (z.B. `user.role`, `room.config`) ohne hartcodierte Listen.
- UC-003: ProjectCreationFix - Wiederherstellung der Projekt-Neuerstellung über das Menü. Implementierung von `newProject` in `Editor.ts` inklusive Benutzerbestätigung und Initialisierung einer SSoT-konformen Blueprint-Stage via `createDefaultProject`. Verknüpfung des 'new-project' Actions im `EditorMenuManager.ts`. (Editor.ts, EditorMenuManager.ts)
- UC-004: CMD-Workflow-Migration - Bereitstellung von CMD-Batch-Dateien (`run_tests.bat`, `validate_project.bat`, `fix_ssot.bat`) als Fallback für beschädigte PowerShell-Umgebungen (`ServicePointManager`-Fehler). Ermöglicht die Fortführung der Projekt-Validierung gemäß Richtlinien.
- [2026-02-27] PascalGeneratorRefactoring: Modularisierung des `PascalGenerator.ts` in [PascalCodeGenerator.ts](file:///c:/Users/rolfr/.gemini/antigravity/scratch/game-builder-v1/src/editor/PascalCodeGenerator.ts) (`generateFullProgram`, `generateProcedure`) und [PascalCodeParser.ts](file:///c:/Users/rolfr/.gemini/antigravity/scratch/game-builder-v1/src/editor/PascalCodeParser.ts) (`parse`, `parseBodyToSequence`). Einführung der Komponenten-Deklaration (`VAR { STAGE COMPONENTS }`) im generierten Quelltext. Upgrade des Parsers zur Erkennung von komplexen Loops (`WHILE`, `FOR`) und If-Else-Strukturen. Integration des `Logger`-Service (Parser). (PascalGenerator.ts, PascalCodeGenerator.ts, PascalCodeParser.ts)
- [2026-02-26] FlowDeletionAndHealthFix: Behebung der Lösch-Inkonsistenz im Flow-Editor durch ID-basierte Löschung in `FlowGraphManager.ts` und Konsolidierung der Bestätigungs-Dialoge. Bereinigung der `project.json` von Platzhalter-Tasks und Einführung von CMD-Batch-Dateien (run_all.bat) zur System-Validierung bei PowerShell-Problemen.
- [2026-02-26] GlobalVariableRegistrationFix: Regression-Fix für den Variablen-"Gedächtnisverlust" bei `${currentUser.name}` nach Stage-Wechsel. Ursache: `GameRuntime.handleStageChange()` registrierte ALLE Objekte (inkl. gecachte globale Variablen aus `RuntimeStageManager.cachedGlobalObjects`) über `registerObject` neu, was den Runtime-Wert (z.B. `{name:'Rolf'}`) mit dem Komponenten-Proxy (`.name = 'currentUser'`) überschrieb. Fix: Globale Variablen-Komponenten (`isVariable && scope === 'global'`) werden beim `registerObject`-Aufruf übersprungen. Ergänzt die bestehenden Fixes `GlobalVariablePersistence` (clear(false)) und `GlobalElementPersistenceFix` (cachedGlobalObjects).
- [2026-02-26] DeletionConfirmConsolidation: Konsolidierung der Lösch-Bestätigungsdialoge. `InspectorActionHandler.handleDelete` delegiert ohne eigenes `confirm` an den Host. `FlowGraphManager.deleteNodeSilent` erlaubt stilles Löschen ohne erneute Nachfrage. `Editor.removeObjectWithConfirm` nutzt `deleteNodeSilent` für Flow-Elemente. Stellt sicher, dass beim Löschen nur 1x gefragt wird.
- [2026-02-27] P1-001: Core-Type-Safety & TaskExecutor-Expansion - Umfassende Absicherung der Projektstruktur. Entfernung des `any`-Typs von `GameAction` in `types.ts` und Behebung von Build-Fehlern in PascalGenerator.ts, FlowAction.ts, MediatorService.ts, AgentController.ts. Einführung von 8 neuen komplexen Tests in `task_executor.expand.test.ts` (Loops, Verschachtelung, Parameter-Interpolation `${$params.msg}`). Repository-Bereinigung von 17 Legacy-Skripten im Root.
- [2026-02-27] P3-001: StandalonePlayerRefactoring - Vollständige Integration des `StageRenderer` in `player-standalone.ts`. Der UniversalPlayer implementiert nun `StageHost`, wodurch redundante Rendering-Logik entfällt. Ersetzung von `.stage` Referenzen durch `.element` zur Konsistenz mit dem StageHost-Interface. Bereinigung von player.html. Beteiligte Dateien: player-standalone.ts, player.html, StageRenderer.ts.
- [2026-02-27] P3-002: DocRestructuring - Aufteilung des DEVELOPER_GUIDELINES.md Monolythen in spezialisierte Architektur- und Komponenten-Dokumente unter `docs/` (architecture.md, runtime-guide.md, etc.). Aktualisierung der Guidelines mit direkten Links und Fokus auf Kern-Arbeitsanweisungen.
- [2026-02-27] P3-003: Core-Cleanup - Komplette Entfernung von TaskEditor.ts und ActionEditor.ts (gelöscht). Bereinigung der Callsites in InspectorActionHandler.ts und Entkopplung via SWITCH_FLOW_CONTEXT Mediator-Event.
- [2026-02-27] UseCase_Logging_Migration: Vollständige Ersetzung von console.* durch Logger.get(). Einführung eines Filter-Patterns zur Entkopplung von Logger und UseCaseManager. Migration der Run-Mode Logs (HTTP, Actions, Simulation, Interpolation) in spezialisierte UseCases. Verifizierung mittels 57 Regressionstests. Beteiligte Dateien: StandardActions.ts, EditorSimulatorManager.ts, EditorRunManager.ts, PropertyHelper.ts.
- [2026-02-27] Phase 15: Reparatur & Ausbau Manager-Tab. Einführung der "Stages"-Einträge im Manager und Fix der Zeilen-Navigation (onEvent). Ermöglicht direkte Navigation zu Flow-Diagrammen und Stages. Beteiligte Dateien: MediatorService.ts, EditorViewManager.ts.
- [2026-02-27] DashboardFinale: Implementierung des "Layered Dark Design" für das Room-Dashboard. Tabellen nutzen nun gestaffelte Farben (#0f111a Außen / #161922 Innen) mit Radien von 16px/12px. Behebung der "Keine Daten vorhanden" Anzeige durch direkte Injektion von Mock-Daten (Assigned Players, Game Queue) in die `project.json`. (project.json, L147-L295)
- [2026-02-27] ResizingFix: Behebung des Fehlers in `StageInteractionManager.ts`, der das Verändern der Komponentengröße im Editor verhinderte. Korrektur durch Initialisierung von `dragStartRel` im Mouse-Down Event. (StageInteractionManager.ts)
- [2026-02-27] GridFix: Wiederherstellung der Gitter-Steuerung im Inspector. Korrektur des Datenkontexts (`InspectorContextBuilder.ts`), der Pfad-Behandlung (`StageHandler.ts`) und Behebung von `TypeError` (ProjectRegistry) sowie `ReferenceError` (StageHandler).
- [2026-02-28] **Phase 23 & 24: Flow-Editor Robustheit & Bereinigung Geister-Tasks**. Lösung des "Resurrection-Bugs" und Entfernung verwaister Tasks. Implementierung: 1. Filterung im `FlowEditor.ts` (`updateFlowSelector`), sodass nur definierte Tasks im Dropdown erscheinen. 2. Auto-Healing im `FlowSyncManager.ts` (`cleanCorruptTaskData`), das beim Laden verwaiste `flowCharts` ohne Task-Definition aus der `project.json` entfernt. 3. Filterung im `MediatorService.ts` (`getTasks`), die Library-Tasks ohne aktive Verwendung (`usageCount == 0`) im Manager-Tab ausblendet. (FlowEditor.ts, FlowSyncManager.ts, MediatorService.ts, project.json)
- [2026-03-01] StageInspectorCleanup: Bereinigung der `inspector_stage.json`. Entfernung der Sektionen Variables, Tasks, Actions und Objects (Stage) zur Verbesserung der Übersichtlichkeit. Der "+ Add Variable" Button wurde ebenfalls entfernt, da diese Elemente in der Stage-Ansicht nicht mehr benötigt werden. (public/inspector_stage.json)
- [2026-03-02] ActionDeletionBugfix (v3.15.2): Vollständige Korrektur der Action-Löschung im Flow-Diagramm. Fix der Case-Sensitivity bei Node-Typen ('action' vs 'Action') in `FlowGraphManager.ts` und `ProjectRegistry.ts`. Korrektur der Referenzprüfung in `ProjectRegistry.ts`, um auch Kleinschreibung zu unterstützen. Konsolidierung der Lösch-Bestätigungsdialoge in `Editor.ts` zur Vermeidung von Doppel-Prompts. Verifiziert durch Deep-Trace-Browsertest.
- [2026-03-01] ActionDeletionFixV2 (v3.15.0): Korrektur des `deleteAction` Fehlers im `ActionRefactoringService.ts`, durch den `DataAction`-Knoten bei der Löschung übersprungen wurden. Vollständige Bereinigung der `project.json` von verwaisten `Action1/2` Definitionen in allen Stages (Login, Dashboard, Blueprint). Verifiziert durch erweiterten `action_crud.test.ts` und workspace-weiten Content-Scan.
UseCase: FlowEditorNavigationFix (Tiefen-Fix)
Files: Editor.ts (L383+), EditorCommandManager.ts (L190+), FlowElement.ts (L480+), FlowTask.ts (L155+), FlowNodeHandler.ts (L43+)
Scope: Umstellung der gesamten Umbenennungs-Logik von UI-gesteuert auf zentral gesteuert (CommandManager).
