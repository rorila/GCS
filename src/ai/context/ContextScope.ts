/**
 * ContextScope
 *
 * Re-Export des Scopes, der für Kontext-Operationen verwendet wird.
 * Der kanonische Typ lebt in ../config/AIConfig.ts.
 */

export type { GenerationScope } from '../config/AIConfig';
