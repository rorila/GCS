import { AIImplementationPlan } from '../config/AIConfig';

/**
 * AIPlan
 *
 * Re-Export der Plan-Datentypen. Hier können später spezifische
 * Planner-Erweiterungen ergänzt werden.
 */

export type AIPlan = AIImplementationPlan;
export type { AIImplementationPlan };
